import type { OpenDevToolsOptions } from 'electron'


export const devtoolsOpenOptions: OpenDevToolsOptions = { mode: 'right', activate: true }


export const shellDevtoolsOpenOptions: OpenDevToolsOptions = { mode: 'detach', activate: true }
